/// App-wide constants (flags, keys, timeouts).
abstract final class AppConstants {
  static const String appName = 'تم';
  static  String deviceToken  = '';
  static const Duration apiTimeout = Duration(seconds: 90);

}
